# The base URL of the deployed backend server (without /api suffix)
VITE_BASE_URL=http://localhost:5000

# The full API URL of the backend server (with /api suffix)
VITE_API_URL=http://localhost:5000/api
